package com.siide.linkup.feature.profile.domain.model;

/**
 * Top-level grouping of interests. Surfaced to the UI for chip layout / icons.
 * V3 might add subcategories; for MVP a flat enum is enough.
 */
public enum InterestCategory {
    SPORT,
    CULTURE,
    TECH,
    FOOD,
    OUTDOOR
}
