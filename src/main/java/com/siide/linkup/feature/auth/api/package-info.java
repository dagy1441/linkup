/**
 * Published API of the Auth module. Other feature modules depend on these contracts
 * only — never on internal {@code domain}, {@code application}, or {@code infrastructure}.
 */
@org.springframework.modulith.NamedInterface("api")
package com.siide.linkup.feature.auth.api;
